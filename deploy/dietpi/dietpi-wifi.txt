# DietPi Wi-Fi credentials for a headless first boot.
# Copy to the FAT boot partition as /boot/dietpi-wifi.txt and fill in your network.
# Up to 8 entries ([0]..[7]) are supported; the first reachable one is used.

aWIFI_SSID[0]='YourNetworkSSID'
aWIFI_KEY[0]='YourNetworkPassword'
aWIFI_KEYMGR[0]='WPA-PSK'
