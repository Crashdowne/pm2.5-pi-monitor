# Keep the systemd journal in RAM to spare the SD card. The SQLite database at
# /var/lib/pm25 stays the durable store; logs are transient and reset on reboot.
[Journal]
Storage=volatile
RuntimeMaxUse=32M
ForwardToSyslog=no
